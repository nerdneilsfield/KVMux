# Vendored dependencies

Each vendored dependency gets its own directory containing:

- only source and build files required by KVMux;
- an exact upstream version or commit record;
- the unmodified upstream license and required notices; and
- local changes documented in a README.

Do not add submodules, package-manager state, generated binaries, or CMake
network downloads here.
